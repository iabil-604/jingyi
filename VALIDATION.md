# 验证边界

当前版本的证据分为两层：

1. `validation/sillytavern-1.18.0.snapshot.json` 来自 SillyTavern `1.18.0` 标签源码审计，证明所调用的上下文符号和事件存在；
2. `npm run validate` 执行本地语法、核心行为、清单和能力契约检查。

这些检查不能替代真实宿主验收。GitHub 发布前仍需在 SillyTavern 1.18.0 中逐项验证：

- 从 Git URL 安装并刷新后扩展正常激活；
- 魔法棒入口与扩展设置入口均能打开控制中心；
- 控制中心不会在启动时自动遮挡聊天；
- 主页面、提示词页、设置页、日志页均可切换和保存；
- “跟随当前连接”和“独立副 API”各完成一次测试翻译；
- 新回复、切换已有滑动页、编辑、切换聊天期间的写回行为正确；
- 下一次主模型请求中不包含带不可见标记的中文镜像；
- 控制中心内的更新按钮能在真实 Git 安装中完成检查、安装和刷新；
- 禁用、启用、更新和清理扩展后没有重复事件或残留面板。

## 本轮本地验收

- 路由回执：`consult-tavernweave-library`、`sillytavern-extension-dev`、`code-quality-workflow`；资料快照 `2026-08-18`；采用 A0、D5、E5 的扩展契约、控制中心、表单和移动端边界；代码质量复核只返回 ST-A0；目录候选仍为 proposed，未采用；
- 目标：交付 0.8.0，在保留简体中文完整规则包的同时加入每方案目标语言、来源语言逐段自动识别、统一重试、标签检查和最新楼层安全合并；
- 红线：不依赖 TavernHelper；未复制参考插件的源代码、素材或视觉资产；提示词按已获授权的长规范与分层检查写法为正文翻译重新编写；程序负责的提取、排除、包裹、标记、写回和日志机制不进入模型提示词；模型的 `<thinking>` / `<think>` 内容不写进聊天正文；不在真实宿主验收前声称完全兼容；
- 数据迁移：设置结构升级为 schema 8；旧 `bodyTag` 自动迁为 `bodyTags`，默认值仍只有 `story_scene`；未修改过的 v0.6/v0.7 默认提示词升级为新版，用户自定义提示词继续保留；旧方案自动取得默认目标语言“简体中文”；
- 提取边界：每个已配置正文标签只选最后一组完整内容；不同标签共同参与同一次首次请求；标签输入接受名称或完整尖括号形式；排除块支持嵌套、自闭合和原位保留；相互交叉而无法安全还原的标签会明确报错；
- 分段边界：完整空行才创建新段落；段内换行、首尾空白和原文顺序均保留；每个空行段落只生成一个目标语言镜像块；
- 调用边界：正常首次翻译把全部选中正文一次发送，不按标签或段落拆批；返回不完整或命中绝对禁用表达时仍沿用恢复与定向修正流程，不伪造缺失译文；
- 并发写回：开始请求时记录的语义签名只包含实际待译段落；写回前重新读取最新楼层。排除标签内容或正文标签外内容变化时在最新结构上合并，真正待译正文变化时按统一预算重译；不使用固定等待或持续抢写；
- 自动回归：35 项通过，覆盖设置/提示词迁移、目标语言变量、非中文通用规则、多标签末组选择、标签检查、空行分段、段内换行、嵌套与自闭合排除、语义签名、整段前后缀、部分写回恢复、可见 think/thinking 解析、提示词职责边界、世界书上下文、参数排除、模型列表和生命周期契约；
- 本地浏览器结果：v0.8.0 控制中心可打开；主页面正确识别 2 段；提示词页 10 个标准条目默认全部收起且一次只展开一个，目标语言切到 English 后显示通用规则提示；设置页“检查当前楼层”报告 `<story_scene>` 1 组、采用第 1 组、2 个可译段落；副 API 请求中途模拟插入 `<image_prompt data-preview="late-insert"/>` 后标签原位保留、2 段译文完整写回，并产生 `translation.rebase` 日志；桌面对话框 client/scroll 宽度均为 761px；390×844 下对话框、内容和文本域无横向溢出；浏览器控制台无警告或错误；
- 通用扩展清单脚本会把带 `?v=0.8.0` 的 manifest 资源误判为非 `.js` / `.css` 文件；项目自身检查会去掉查询参数后核对真实文件，并保留查询参数解决更新后的混合缓存问题；
- 尚未关闭：真实 SillyTavern 中从 GitHub 更新到 0.8.0 后的入口恢复、真实副 API、真实世界书激活、滑动页/聊天切换保存流程，以及与实际文生图扩展同时写入同一楼层的验收。

## v0.8.1 本地验收

- 目标：修复独立副 API 拉取模型后的首次选择体验，并让空回、截断、格式漂移问题能够取得每次请求的完整返回证据；本轮没有修改翻译提示词、请求拆分、解析、重试或写回策略；
- 模型列表：拉取成功后不再把排序后的第一个模型写入当前预设；原生下拉框显示接口返回的全部模型，并保持“请选择”状态，用户明确选择后才同步到手动模型输入框；接口无法列出模型时仍可手动填写；
- 诊断记录：每次成功收到副 API 返回后，在解析前保存完整原始返回；对象响应保留 `content`、`reasoning`、`reasoning_content`、`thinking` 及其他原始字段，字符串响应原样保留；完整返回默认收起并可单条复制；
- 隐私边界：安全摘要不包含完整返回；完整日志明确提示可能含正文和模型思考，常见凭据字段与 Bearer/查询参数密钥形态会隐藏；达到本地存储预算时整条淘汰最旧记录，不截断仍保留的单条响应；
- 自动回归：36 项通过，新增完整返回保存、安全摘要隔离、凭据脱敏、模型列表不自动选择以及完整日志入口的覆盖；语法检查和项目清单检查通过；
- 本地浏览器结果：v0.8.1 模拟宿主中拉取到 `translator-pro`、`translator-small` 后，两项同时可见且没有自动选中；选择 `translator-small` 后手动输入框同步；测试请求生成 `translation.raw-response` 日志，展开后可见完整 `content` 与 `reasoning`；
- 已知验证边界：上述为模拟宿主和模拟 OpenAI 兼容端点的本地证据，尚未证明真实 SillyTavern 安装更新、真实副 API 的思考字段形态或浏览器存储配额表现。跟随当前连接时，扩展只能记录 SillyTavern `generateRaw` 实际返回给扩展的字符串；宿主未暴露的隐藏思考无法由扩展补取。

## v0.8.2 本地验收

- 目标：把平行线、控制标签、装饰线和段内换行的机械处理从副模型提示词移回脚本，同时保持正常首次翻译只有一次整批请求；
- 数据迁移：设置结构升级为 schema 9，旧设置原样保留并补入空白的“原样保留白名单”；未修改翻译提示词及用户自定义方案；
- 分段与写回：完整空行仍决定外层段落；段内语义行使用连续内部 ID，一次发送给副 API，返回后按原行序组合进同一个多行镜像块；新镜像块使用成对不可见边界，生成拦截、重新翻译和部分恢复均能完整识别；旧版单行不可见标记继续支持剥离与单行恢复；
- 结构保护：排除标签整块原样保留；其他转义或未转义标签视作透明结构，标签语法不进入翻译单元；纯符号、纯边框和仅含标签的行由内置规则原样保留；
- 自定义白名单：支持整行、`prefix:` 行首和 `/正则/flags` 三种规则；命中行不建立翻译 ID、不发送给副 API且保持原位置；错误正则和空 prefix 会给出行号；暂不支持任意行内子串白名单，行内整块排除继续使用排除标签；
- 检查报告：显示空行段落数、实际翻译行数、自定义白名单命中、内置结构保留行及透明结构标签；
- 自动回归：38 项通过，新增多行镜像剥离/恢复、旧单行标记兼容、白名单三类规则、无效规则、透明容器标签、转义排除标签以及容器闭合位置覆盖；语法、清单一致性和 diff whitespace 检查通过；
- 尚未关闭：真实 SillyTavern 1.18.0 中的更新重载、真实长正文副 API、平行线预设、移动端设置页，以及与实际文生图扩展同时写入同一楼层的联动验收。

## v0.9.0 交付

- 重写通用规范和清单，采用全篇简短核对，取消逐 ID 检查报告；保留单批请求和现有解析协议。
- 旧默认文本按完整内容匹配升级，用户修改不自动覆盖；提供带方案备份的手动采用入口。
- 界面改为紧凑深色浅紫风格，提示词列表默认收起；新增模型搜索和首页当前方案入口。
- 按本轮要求只做语法及现有核心检查；真实模型翻译质量、桌面/手机界面和交互由人工验收。

## v0.10.0 前端重建

- 重写静态界面与整个面板样式，改用五个工作区；提示词条目编辑器移动到独立区域并保留编辑位置。
- 对照上一版本静态界面的 action、设置字段和连接字段，三类控制均无遗漏。
- 本地模拟宿主快速查看了桌面工作台及窄屏规则编辑，修正编辑聚焦引起的外层窗口滚动；正式酒馆和人工操作体验待验收。
- 提示词文本、翻译调用、提取/写回算法没有修改。公开版本 0.10.0。

- 追加五主题、方案导入导出、更新入口与移动端留边调整后，按用户明确要求停止浏览器操作，仅做 JavaScript 语法与文件差异检查；追加部分的外观、导入导出和更新交互由用户验收。
